import { HintDropdownDirective } from './hint-dropdown.directive';

describe('HintDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new HintDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
